# MIDA STORE — Render Ready

Upload the FILES directly to the ROOT of your GitHub repository. Do not upload this ZIP as the only project file.

Render:
- Runtime: Node
- Build Command: npm install
- Start Command: npm start

The real PhonePe/GPay merchant integration and player-name verification require authorized services. Never put OTPs, UPI PINs, bank passwords, or payment secrets into this project.
